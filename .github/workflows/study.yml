name: myfirst
on:
  push:
jobs:
  build:
    name: first stage
    runs-on: ubuntu-latest
    steps:
      - name: Check Out
        uses: actions/checkout@v2

      - name: build package
        run: |
          echo "Ima on build package"
    
